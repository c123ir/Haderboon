# ساختار فرانت‌اند ایجنت هادربون

## ۱. ساختار پوشه‌ها و فایل‌ها

```
frontend/
├── public/                  # فایل‌های استاتیک
│   ├── index.html           # فایل HTML اصلی
│   └── assets/              # تصاویر و منابع استاتیک
├── src/                     # کدهای اصلی فرانت‌اند
│   ├── components/          # کامپوننت‌های قابل استفاده مجدد
│   │   ├── common/          # کامپوننت‌های عمومی
│   │   ├── layout/          # کامپوننت‌های چیدمان
│   │   ├── auth/            # کامپوننت‌های احراز هویت
│   │   ├── project/         # کامپوننت‌های مرتبط با پروژه
│   │   ├── document/        # کامپوننت‌های مرتبط با مستندات
│   │   └── chat/            # کامپوننت‌های مرتبط با چت
│   ├── context/             # کانتکست‌های React
│   │   ├── AuthContext.tsx  # کانتکست احراز هویت
│   │   └── ProjectContext.tsx # کانتکست پروژه
│   ├── hooks/               # هوک‌های سفارشی
│   │   ├── useAuth.ts       # هوک برای احراز هویت
│   │   ├── useProjects.ts   # هوک برای مدیریت پروژه‌ها
│   │   └── useChat.ts       # هوک برای مدیریت چت
│   ├── pages/               # صفحات اصلی برنامه
│   │   ├── Home.tsx         # صفحه اصلی
│   │   ├── auth/            # صفحات احراز هویت
│   │   ├── dashboard/       # صفحات داشبورد
│   │   ├── project/         # صفحات مرتبط با پروژه
│   │   ├── document/        # صفحات مرتبط با مستندات
│   │   └── chat/            # صفحات مرتبط با چت
│   ├── services/            # سرویس‌های ارتباط با API
│   │   ├── api.ts           # تنظیمات پایه Axios
│   │   ├── authService.ts   # سرویس احراز هویت
│   │   └── projectService.ts # سرویس مدیریت پروژه
│   ├── types/               # تعاریف تایپ‌های TypeScript
│   ├── utils/               # توابع و ابزارهای کمکی
│   ├── styles/              # استایل‌های کلی
│   │   └── index.css        # استایل‌های اصلی و تنظیمات Tailwind
│   ├── routes/              # تنظیمات مسیریابی
│   ├── App.tsx              # کامپوننت اصلی برنامه
│   └── index.tsx            # نقطه ورود React
├── package.json             # وابستگی‌ها و اسکریپت‌ها
├── tsconfig.json            # تنظیمات TypeScript
└── tailwind.config.js       # تنظیمات Tailwind CSS
```

## ۲. کانتکست احراز هویت

```tsx
// src/context/AuthContext.tsx
import React, { createContext, useState, useEffect, ReactNode } from 'react';
import { User } from '../types/auth.types';
import authService from '../services/authService';
import { getTokenFromStorage, setTokenToStorage, removeTokenFromStorage } from '../utils/localStorage';

interface AuthContextType {
  user: User | null;
  isAuthenticated: boolean;
  isLoading: boolean;
  login: (email: string, password: string) => Promise<void>;
  register: (username: string, email: string, password: string) => Promise<void>;
  logout: () => void;
}

export const AuthContext = createContext<AuthContextType>({
  user: null,
  isAuthenticated: false,
  isLoading: true,
  login: async () => {},
  register: async () => {},
  logout: () => {},
});

export const AuthProvider: React.FC<{ children: ReactNode }> = ({ children }) => {
  const [user, setUser] = useState<User | null>(null);
  const [isLoading, setIsLoading] = useState(true);
  
  // بررسی وضعیت احراز هویت هنگام بارگذاری اولیه
  useEffect(() => {
    const initAuth = async () => {
      const token = getTokenFromStorage();
      
      if (token) {
        try {
          const userData = await authService.getCurrentUser();
          setUser(userData);
        } catch (error) {
          removeTokenFromStorage();
          setUser(null);
        }
      }
      
      setIsLoading(false);
    };
    
    initAuth();
  }, []);
  
  // توابع احراز هویت
  const login = async (email: string, password: string) => {
    setIsLoading(true);
    try {
      const response = await authService.login(email, password);
      setTokenToStorage(response.token);
      setUser(response.user);
    } finally {
      setIsLoading(false);
    }
  };
  
  const register = async (username: string, email: string, password: string) => {
    setIsLoading(true);
    try {
      const response = await authService.register(username, email, password);
      setTokenToStorage(response.token);
      setUser(response.user);
    } finally {
      setIsLoading(false);
    }
  };
  
  const logout = () => {
    removeTokenFromStorage();
    setUser(null);
  };
  
  return (
    <AuthContext.Provider
      value={{
        user,
        isAuthenticated: !!user,
        isLoading,
        login,
        register,
        logout,
      }}
    >
      {children}
    </AuthContext.Provider>
  );
};
```

## ۳. سرویس API و احراز هویت

```typescript
// src/services/api.ts
import axios from 'axios';
import { getTokenFromStorage, removeTokenFromStorage } from '../utils/localStorage';

const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:5550/api/v1';

const api = axios.create({
  baseURL: API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// افزودن توکن به هدر درخواست‌ها
api.interceptors.request.use(
  (config) => {
    const token = getTokenFromStorage();
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  }
);

// مدیریت خطاهای احراز هویت
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      removeTokenFromStorage();
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export default api;
```

```typescript
// src/services/authService.ts
import api from './api';
import { LoginResponse, RegisterResponse } from '../types/auth.types';

const authService = {
  // ورود کاربر
  async login(email: string, password: string): Promise<LoginResponse> {
    try {
      const response = await api.post<LoginResponse>('/auth/login', {
        email,
        password,
      });
      return response.data;
    } catch (error: any) {
      throw new Error(
        error.response?.data?.message || 'خطا در ورود به سیستم'
      );
    }
  },

  // ثبت‌نام کاربر
  async register(
    username: string,
    email: string,
    password: string
  ): Promise<RegisterResponse> {
    try {
      const response = await api.post<RegisterResponse>('/auth/register', {
        username,
        email,
        password,
      });
      return response.data;
    } catch (error: any) {
      throw new Error(
        error.response?.data?.message || 'خطا در ثبت‌نام'
      );
    }
  },

  // دریافت اطلاعات کاربر فعلی
  async getCurrentUser() {
    try {
      const response = await api.get('/auth/me');
      return response.data.user;
    } catch (error: any) {
      throw new Error(
        error.response?.data?.message || 'خطا در دریافت اطلاعات کاربر'
      );
    }
  },
};

export default authService;
```

## ۴. کامپوننت مسیر محافظت شده

```tsx
// src/routes/ProtectedRoute.tsx
import React from 'react';
import { Navigate } from 'react-router-dom';
import { useAuth } from '../hooks/useAuth';
import Loading from '../components/common/Loading';

interface ProtectedRouteProps {
  element: React.ReactElement;
  redirectPath?: string;
}

const ProtectedRoute: React.FC<ProtectedRouteProps> = ({
  element,
  redirectPath = '/login',
}) => {
  const { isAuthenticated, isLoading } = useAuth();
  
  // نمایش لودینگ در حین بررسی وضعیت احراز هویت
  if (isLoading) {
    return <Loading />;
  }
  
  // اگر کاربر احراز هویت نشده باشد، به صفحه ورود هدایت می‌شود
  if (!isAuthenticated) {
    return <Navigate to={redirectPath} replace />;
  }
  
  // در غیر این صورت، المان مورد نظر نمایش داده می‌شود
  return element;
};

export default ProtectedRoute;
```

## ۵. تنظیمات Tailwind CSS

```javascript
// tailwind.config.js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
    "./public/index.html",
  ],
  darkMode: 'class', // امکان تغییر بین حالت روشن و تاریک
  theme: {
    extend: {
      fontFamily: {
        vazir: ['Vazirmatn', 'sans-serif'],
      },
      colors: {
        primary: {
          500: '#0ea5e9',
          600: '#0284c7',
          700: '#0369a1',
        },
        secondary: {
          500: '#d946ef',
          600: '#c026d3',
          700: '#a21caf',
        },
      },
    },
  },
  plugins: [
    require('@tailwindcss/forms'),
  ],
}
```

## ۶. استایل‌های اصلی

```css
/* src/styles/index.css */
@import 'tailwindcss/base';
@import 'tailwindcss/components';
@import 'tailwindcss/utilities';

@layer base {
  html {
    direction: rtl;
    font-family: 'Vazirmatn', sans-serif;
  }
  
  body {
    @apply bg-gray-50 text-gray-900 dark:bg-gray-900 dark:text-gray-100;
  }
}

@layer components {
  .card {
    @apply bg-white dark:bg-gray-800 rounded-lg shadow-md p-4 md:p-6;
  }
  
  .btn-primary {
    @apply px-4 py-2 rounded-md font-medium bg-primary-500 text-white 
           hover:bg-primary-600 transition-colors duration-200;
  }
  
  .input {
    @apply w-full px-3 py-2 border border-gray-300 dark:border-gray-700 
           rounded-md focus:outline-none focus:ring-2 focus:ring-primary-500;
  }
}
``` 